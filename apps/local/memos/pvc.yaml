apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos
  namespace: memos
  labels:
    k8up.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi