apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos
  namespace: memos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi