apiVersion: v1
kind: Service
metadata:
  name: remote-cluster-a-service
  namespace: external
  annotations:
    tailscale.com/tailnet-fqdn: "example-backend.${tailscale_domain_name}"
    tailscale.com/proxy-group: "egress-proxies"
spec:
  type: ExternalName
  externalName: placeholder
  ports:
  - port: 80
    targetPort: 80