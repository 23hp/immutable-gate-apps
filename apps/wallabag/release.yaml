---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wallabag
  namespace: wallabag
spec:
  interval: 24h
  install:
    strategy:
      name: RetryOnFailure
  upgrade:
    strategy:
      name: RetryOnFailure
  chartRef:
    kind: OCIRepository
    name: wallabag
    namespace: flux-system
  values:
    wallabag:
      existingSecret: symfony-secret
      domainName: "https://wallabag.${domain_name}"
      extraEnv:
        - name: "POPULATE_DATABASE" # for first time installation and single instance only
          value: "True"
        - name: "TZ"
          value: "Europe/Berlin"
    postgresql:
      enabled: false
    database:
      external:
        host: postgres-shared-rw.database.svc.cluster.local
        name: wallabag
        username: wallabag_user
        existingSecret: wallabag-db-credentials