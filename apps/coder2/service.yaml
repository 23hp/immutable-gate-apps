apiVersion: v1
kind: Service
metadata:
  name: coder-service
  namespace: coder2
  annotations:
    tailscale.com/tailnet-fqdn: "coder-coder.tail1fce8c.ts.net"
    tailscale.com/proxy-group: "egress-proxies"
spec:
  type: ExternalName
  externalName: placeholder
  ports:
  - port: 80
    targetPort: 80