---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: configuration
  namespace: flux-system
spec:
  interval: 8h
  retryInterval: 1h
  timeout: 20m
  prune: true
  path: "./configuration/local"
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: infrastructure
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: init-config
  decryption:
    provider: sops
    secretRef:
      name: sops-age