---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy
  namespace: envoy-gateway-system
spec:
  interval: 24h
  chartRef:
    kind: OCIRepository
    name: envoy
    namespace: flux-system
