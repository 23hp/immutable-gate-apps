apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: garage
  namespace: flux-system
spec:
  interval: 36h
  url: oci://ghcr.io/quenchworks/charts/garage
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  ref:
    semver: "0.0.11"