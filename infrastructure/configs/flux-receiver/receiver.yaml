---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: flux-receiver
  namespace: flux-system
spec:
  interval: 1d
  type: generic
  secretRef:
    name: receiver-token
  resources:
    - kind: GitRepository
      name: flux-system